# 对话记录 — 2026年5月16日

> 项目：Firefly（Astro 博客）
> 话题：浏览器缓存、Chrome 扩展干扰、本地开发 vs 线上部署

---

## 问题 1：`pnpm dev` 更新后，VSCode 内置浏览器正常，外部浏览器看不到更新

### 初步排查
- **猜测**：浏览器缓存问题
- **尝试**：
  1. `Ctrl + Shift + R` 强制刷新 → 无效
  2. DevTools → Network → 勾选 "Disable cache" → 无效
  3. 清除站点数据 → 无效
  4. 无痕/隐私模式 → 无效
  5. 禁用 Swup 缓存（`astro.config.mjs` 中 `cache: false`）→ 无效
  6. 用 `127.0.0.1:4321` 替代 `localhost:4321` → **打不开**（说明 dev server 只监听 IPv6 `::1`）

### 深入排查
- 检查 Network 面板 Response，确认服务器返回的是新内容
- 发现 Console 中有 **Adobe Acrobat 扩展** (`efaidnbmnnnibpcajpcglclefindmkaj`) 注入字体的日志

### 根因 & 解决
- **关闭所有 Chrome 扩展**后，外部浏览器恢复正常
- Chrome 扩展（特别是 Adobe Acrobat、广告拦截类）会拦截/缓存页面响应，导致看不到最新内容
- 扩展逐個重新开启后问题不再复现（因为缓存已被刷新），但下次修改代码后可能复发

### 建议
- 创建一个**纯净的 Chrome Profile** 用于开发调试：
  1. `chrome://settings/people` → 添加个人资料
  2. 命名为 "Dev 调试"
  3. 不装任何扩展，专门用于 `localhost` 调试

---

## 问题 2：`localhost:4321` 正常，但 `blog.perplexboy.com` 仍显示旧版

### 根因
- `localhost:4321` 是本地开发服务器（`pnpm dev`），修改代码即时生效
- `blog.perplexboy.com` 是线上生产站点，托管在 GitHub Pages，需要**构建并部署**才会更新
- 两者是完全独立的，修改本地代码不会自动同步到线上

### 项目部署架构
- **部署工具**：GitHub Actions（`.github/workflows/deploy.yml`）
- **触发条件**：推送到 `master` 分支
- **构建输出**：`dist/` 目录
- **部署目标**：`pages` 分支（GitHub Pages）
- **域名**：`blog.perplexboy.com` 指向 GitHub Pages
- **额外配置**：`wrangler.toml`（Cloudflare，当前未在 deploy workflow 中使用）

### 部署步骤
1. 提交代码：`git add . && git commit -m "更新站点配置"`
2. 推送到远端：`git push --set-upstream origin master`
3. 等待 GitHub Actions 中 `Deploy to Pages Branch` 工作流完成

### 遇到的问题
- `git commit` 时显示 `nothing to commit, working tree clean`——改动已在之前提交过
- `git push` 时报 `no upstream branch`——需要 `git push --set-upstream origin master`

---

## 技术要点总结

| 问题 | 原因 | 解决方案 |
|------|------|----------|
| 外部浏览器看不到 dev 更新 | Chrome 扩展缓存/篡改页面响应 | 关闭扩展 或 使用纯净 Chrome Profile |
| 127.0.0.1 打不开 dev server | Astro dev server 默认只监听 IPv6 `::1` | 使用 `localhost` 或在 `vite.server.host` 配置 `0.0.0.0` |
| 线上站点不更新 | 本地 dev 和线上部署是独立的 | `git push` 触发 GitHub Actions 自动部署 |
